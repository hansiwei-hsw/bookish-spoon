# Python科学计算器 v2.0 调试日志

## 调试轮次 #1: 浮点数精度问题修复

### Bug现象
- `sin(30°)` 返回 `0.49999999999999994`（预期: 0.5）
- `cos(90°)` 返回 `6.123233995736766e-17`（预期: 0）
- 格式化输出时显示极小的科学计数法数值

### 复现步骤
1. 初始化 CalculatorCore
2. 调用 `core.sin(30, True)` 或 `core.cos(90, True)`
3. 直接打印原始结果

### 根因分析
**文件**: `result_formatter.py:27-34`
- 原代码使用 `abs(value) == 0` 进行精确比较
- 未考虑浮点数计算精度误差（如π的近似值导致三角函数计算结果不精确）
- 极小值（如1e-17量级）被错误地当作有效数值

### 修复方案
1. 新增类常量 `ZERO_TOLERANCE = 1e-10` 作为零值容差
2. 修改 `format_number` 函数:
   - 使用 `abs(value) < ZERO_TOLERANCE` 替代 `abs(value) == 0`
   - 增加四舍五入后的二次校验
   - 整数判断也使用容差比较：`abs(value - int(value)) < ZERO_TOLERANCE`

### 验证结果
修复后:
- `cos(90°)` 格式化后显示为 `0`
- `sin(30°)` 格式化后显示为 `0.5`
- `sin(90°)` 格式化后显示为 `1`

### 预防措施
- 所有浮点数比较都应使用容差范围
- 在格式化模块中集中处理精度问题

---

## 调试轮次 #2: 三角函数边界值校验

### Bug现象
- `tan(90°)` 计算时未报错，返回极大值（预期应报错）

### 复现步骤
1. 初始化 CalculatorCore
2. 调用 `core.tan(90, True)`
3. 观察结果（应为无定义）

### 根因分析
**文件**: `calculator_core.py:tan()` 函数
- tan函数依赖cos值，当cos值为0时tan无定义
- 原代码虽有检查，但cos值可能为极小值而非精确0

### 修复方案
在tan函数中使用容差比较:
```python
if abs(math.cos(x)) < 1e-10:
    raise ValueError(f"tan({x}) 无定义")
```

### 验证结果
- 调用 `tan(90°)` 正确抛出 `ValueError: tan(x) 无定义`

---

## 调试轮次 #3: 兼容模式表达式解析

### Bug现象
- 表达式 `100*-2` 解析失败
- 负数开头的表达式如 `-3+5` 解析异常

### 复现步骤
1. 初始化 InputValidator
2. 调用 `validator.parse_compatibility_input('-3+5')`
3. 观察解析结果

### 根因分析
**文件**: `input_validator.py`
- 原正则表达式未正确处理第一个数为负数的情况
- 解析逻辑从位置0开始搜索运算符，会匹配到负号

### 修复方案
1. 修改正则表达式：`^-?\d+(\.\d+)?[+\-*/]-?\d+(\.\d+)?$`
2. 解析时从位置1开始搜索运算符：`enumerate(expr[1:], 1)`

### 验证结果
- `-3.5+4.2` 正确解析为 `(-3.5, '+', 4.2)`
- `100*-2` 正确解析为 `(100.0, '*', -2.0)`

---

## 调试轮次 #4: 阶乘边界值处理

### Bug现象
- 小数阶乘如 `factorial(3.5)` 未正确校验
- 极大数阶乘导致内存溢出

### 复现步骤
1. 调用 `core.factorial(3.5)`
2. 观察是否正确抛出异常

### 修复方案
1. 增加整数校验：`not float(n).is_integer()`
2. 增加最大值限制：`n > 1000` 时抛出 OverflowError

### 验证结果
- `factorial(3.5)` 正确抛出 `ValueError: 阶乘要求n为整数`
- `factorial(-5)` 正确抛出 `ValueError: 阶乘要求 n >= 0`
