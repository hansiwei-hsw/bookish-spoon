# Python科学计算器 v2.0 使用说明

## 功能列表

### 基础运算
- 加法 (+)
- 减法 (-)
- 乘法 (*)
- 除法 (/)

### 科学运算
- 三角函数：sin, cos, tan（支持角度/弧度切换）
- 对数运算：自然对数(ln)、常用对数(log10)
- 幂运算：x^y
- 开方运算：√x
- 阶乘：n!
- 绝对值：|x|
- 倒数：1/x
- 指数函数：e^x

### 系统功能
- 交互式菜单系统
- 输入校验与异常处理
- 历史记录管理（保存/查看/导出）
- 自定义配置（小数位数、角度模式等）
- 兼容模式（支持旧版本输入习惯）

---

## 操作步骤

### 1. 启动程序
```bash
python main.py
```

### 2. 主菜单操作
启动后显示主菜单，输入对应数字选择功能：
- `1`: 基础四则运算
- `2`: 三角函数运算
- `3`: 对数与指数运算
- `4`: 幂运算与开方
- `5`: 阶乘与其他运算
- `6`: 历史记录管理
- `7`: 系统设置
- `0`: 退出程序

**提示：任何时候输入 `q` 可返回上级菜单**

### 3. 基础四则运算
1. 选择运算类型（1-4）
2. 依次输入两个数字
3. 系统显示计算结果并自动保存到历史记录

### 4. 三角函数运算
1. 选择 sin/cos/tan
2. 输入角度或弧度值（取决于当前模式）
3. 可随时切换角度/弧度模式

### 5. 系统设置
进入设置菜单可配置：
- 小数位数（0-10位，默认4位）
- 角度/弧度模式（默认角度）
- 兼容模式开关
- 科学计数法阈值

---

## 兼容模式

### 开启方法
1. 主菜单 → `7`（系统设置）→ `3`（兼容模式切换）
2. 或直接修改配置文件 `calculator_config.json`

### 使用方法
兼容模式支持旧版本的简单表达式输入，如：
- `3+5`
- `-2.5*4`
- `10/3`
- `100-50`

直接输入表达式后按回车即可得到结果。

---

## 历史记录功能

### 查看历史
主菜单 → `6` → `1`：查看最近10条记录

### 导出历史
主菜单 → `6` → `2`：导出到 `calculator_history_export_YYYYMMDD_HHMMSS.txt`

### 清空历史
主菜单 → `6` → `3`：清空所有历史记录（需确认）

---

## 配置说明

### 配置文件
配置保存在 `calculator_config.json` 中：
```json
{
  "use_degree": true,
  "decimal_places": 4,
  "compatibility_mode": false,
  "scientific_threshold": 1000000.0
}
```

### 配置项说明
- `use_degree`: true=角度模式, false=弧度模式
- `decimal_places`: 默认保留小数位数（0-10）
- `compatibility_mode`: 兼容模式开关
- `scientific_threshold`: 科学计数法阈值

---

## 输入校验规则

### 数字输入
- 支持整数：`123`, `-456`
- 支持小数：`3.14`, `-0.5`
- 不支持：`1e3`, `NaN`, `Infinity`

### 特殊函数限制
- ln(x), log10(x): 要求 x > 0
- √x: 要求 x ≥ 0
- factorial(n): 要求 n ≥ 0 且为整数
- tan(x): x 不能使余弦值为0
- 除法：除数不能为0

---

## 结果格式化
- 整数结果自动去除小数位：`5.0` → `5`
- 保留4位小数（可配置）：`3.1415926` → `3.1416`
- 超大数自动转换为科学计数法
- 浮点数精度误差自动修正（如 `0.49999999999999994` → `0.5`）

---

## 快捷键与特殊命令
- `q`: 返回上级菜单
- `Ctrl+C`: 中断当前输入
- 菜单选择：输入数字后按回车

---

## 异常处理
系统会自动捕获以下异常并显示友好提示：
- 除数为零
- 无效数字输入
- 超出函数定义域
- 文件读写权限错误
- 计算结果溢出

---

## 缓存机制
系统内置缓存优化：
- 高频三角函数角度/弧度转换结果缓存
- 最近100次计算结果缓存
- 可通过重启程序清除所有缓存

如有问题，请查看调试日志或检查配置文件。
