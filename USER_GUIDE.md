# Python科学计算器 v2.0 使用说明

## 一、功能概述

Python科学计算器 v2.0 是一款面向科研/工程人员的工程级科学计算器，具备以下特性：

- **基础运算**：支持加、减、乘、除四则运算
- **科学运算**：三角函数、对数、幂运算、阶乘等10+类运算
- **历史记录**：自动保存计算历史，支持导出
- **灵活配置**：角度/弧度模式切换、小数位数设置
- **兼容模式**：支持旧版本输入习惯

---

## 二、快速开始

### 环境要求
- Python 3.8 或更高版本
- 无需安装第三方依赖（仅使用标准库）

### 启动方式
```bash
python main.py
```

---

## 三、功能详解

### 3.1 基础运算

支持标准的四则运算：

| 运算符 | 说明 | 示例 |
|--------|------|------|
| + | 加法 | 5 + 3 = 8 |
| - | 减法 | 10 - 4 = 6 |
| * | 乘法 | 6 * 7 = 42 |
| / | 除法 | 15 / 3 = 5 |

**操作步骤：**
1. 主菜单选择 `1. 基础运算`
2. 输入第一个数字
3. 输入运算符（+, -, *, /）
4. 输入第二个数字
5. 查看计算结果

### 3.2 科学运算

#### 3.2.1 三角函数

| 函数 | 说明 | 示例（角度制） |
|------|------|----------------|
| sin(x) | 正弦 | sin(30) = 0.5 |
| cos(x) | 余弦 | cos(60) = 0.5 |
| tan(x) | 正切 | tan(45) = 1.0 |

**注意：** 默认使用角度制，可在设置中切换为弧度制。

#### 3.2.2 反三角函数

| 函数 | 说明 | 输入范围 |
|------|------|----------|
| asin(x) | 反正弦 | -1 到 1 |
| acos(x) | 反余弦 | -1 到 1 |
| atan(x) | 反正切 | 任意实数 |

#### 3.2.3 对数运算

| 函数 | 说明 | 示例 |
|------|------|------|
| ln(x) | 自然对数（底数e） | ln(2.718) ≈ 1 |
| log10(x) | 常用对数（底数10） | log10(100) = 2 |
| log(x, base) | 自定义底数对数 | log(8, 2) = 3 |

**注意：** 对数运算要求真数必须大于0。

#### 3.2.4 幂运算与开方

| 函数 | 说明 | 示例 |
|------|------|------|
| x^y | 幂运算 | 2^10 = 1024 |
| sqrt(x) | 平方根 | sqrt(16) = 4 |

**注意：** sqrt(x) 要求 x ≥ 0。

#### 3.2.5 阶乘运算

计算非负整数的阶乘。

**限制：** 最大支持 170!（更大的值会导致溢出）

**示例：**
- 5! = 120
- 10! = 3628800

#### 3.2.6 其他运算

| 函数 | 说明 | 示例 |
|------|------|------|
| abs(x) | 绝对值 | abs(-5) = 5 |
| exp(x) | e的幂次方 | exp(1) ≈ 2.718 |

### 3.3 历史记录

- 自动保存所有计算记录
- 最多保存100条记录（可在配置中修改）
- 支持导出为txt文件

**操作步骤：**
1. 主菜单选择 `3. 查看历史记录`
2. 查看最近20条记录
3. 可选择导出到文件

### 3.4 系统设置

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| 角度模式 | degree（角度）/ radian（弧度） | degree |
| 小数位数 | 结果显示的小数位数 | 4 |
| 兼容模式 | 开启后使用旧版输入方式 | 关闭 |

---

## 四、兼容模式

兼容模式为旧版本用户提供熟悉的操作方式：

**输入格式：**
```
数字1 运算符 数字2
```

**示例：**
```
请输入表达式: 5 + 3
计算结果: 5 + 3 = 8

请输入表达式: 10 / 2
计算结果: 10 / 2 = 5
```

**开启方式：**
1. 主菜单选择 `4. 系统设置`
2. 选择 `3. 切换兼容模式`

---

## 五、配置文件说明

程序运行时会生成配置文件 `calculator_config.json`：

```json
{
    "angle_mode": "degree",
    "decimal_places": 4,
    "compatibility_mode": false,
    "scientific_notation_threshold": 1e+10,
    "history_max_records": 100,
    "cache_enabled": true,
    "cache_max_size": 100
}
```

| 配置项 | 说明 |
|--------|------|
| angle_mode | 角度模式：degree/radian |
| decimal_places | 小数位数 |
| compatibility_mode | 兼容模式开关 |
| scientific_notation_threshold | 科学计数法阈值 |
| history_max_records | 历史记录最大条数 |
| cache_enabled | 缓存开关 |
| cache_max_size | 缓存最大容量 |

---

## 六、常见问题

### Q1: 为什么 sin(30) 的结果不是 0.5？
**A:** 请检查角度模式设置。如果当前是弧度模式，sin(30) 会将30当作弧度计算。请在设置中切换为角度模式。

### Q2: 阶乘运算提示"参数过大"怎么办？
**A:** 由于浮点数精度限制，阶乘运算最大支持170!。更大的数值会导致溢出。

### Q3: 对数运算报错"参数必须大于0"？
**A:** 对数函数的真数必须为正数。请检查输入值是否正确。

### Q4: 如何清空历史记录？
**A:** 进入系统设置，选择"清空历史记录"选项。

---

## 七、快捷键

| 按键 | 功能 |
|------|------|
| Ctrl+C | 退出当前操作/程序 |
| Enter | 确认输入/继续 |

---

## 八、版本信息

- **版本号：** v2.0
- **发布日期：** 2024年
- **开发团队：** Python科学计算器开发组
